<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="Bucket.App.Views.ThemeSettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:l="using:WinUI3Localizer"
      xmlns:local="using:Bucket.App"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:views="using:Bucket.App.Views"
      dev:BreadcrumbNavigator.IsHeaderVisible="True"
      mc:Ignorable="d">
    <ScrollView Margin="{ThemeResource ContentPageMargin}"
                Padding="{ThemeResource ContentPagePadding}"
                VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10"
                    dev:PanelAttach.ChildrenTransitions="Default"
                    Spacing="5">
            <dev:SettingsCard l:Uids.Uid="/Settings/Theme_AppTheme_Card"
                              HeaderIcon="{dev:BitmapIcon Source=Assets/Fluent/Theme.png}">
                <ComboBox dev:ThemeServiceAttach.ThemeService="{x:Bind local:App.Current.ThemeService}">
                    <ComboBoxItem l:Uids.Uid="/Settings/Theme_Light"
                                  Tag="Light" />
                    <ComboBoxItem l:Uids.Uid="/Settings/Theme_Dark"
                                  Tag="Dark" />
                    <ComboBoxItem l:Uids.Uid="/Settings/Theme_Default"
                                  Tag="Default" />
                </ComboBox>
            </dev:SettingsCard>
            <dev:SettingsCard l:Uids.Uid="/Settings/Theme_Material_Card"
                              HeaderIcon="{dev:BitmapIcon Source=Assets/Fluent/Backdrop.png}">
                <ComboBox dev:ThemeServiceAttach.ThemeService="{x:Bind local:App.Current.ThemeService}">
                    <ComboBoxItem Content="Mica"
                                  Tag="Mica" />
                    <ComboBoxItem Content="Mica Alt"
                                  Tag="MicaAlt" />
                    <ComboBoxItem Content="Acrylic"
                                  Tag="Acrylic" />
                    <ComboBoxItem Content="Acrylic Thin"
                                  Tag="AcrylicThin" />
                </ComboBox>
            </dev:SettingsCard>
            <dev:SettingsCard l:Uids.Uid="/Settings/Theme_AccentColor_Card"
                              ActionIcon="{dev:BitmapIcon Source=Assets/Fluent/External.png}"
                              HeaderIcon="{dev:BitmapIcon Source=Assets/Fluent/Color.png}"
                              IsClickEnabled="True"
                              LaunchUri="ms-settings:colors" />
        </StackPanel>
    </ScrollView>
</Page>