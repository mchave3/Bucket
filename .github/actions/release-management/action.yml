name: '🚀 Release Management'
description: 'Create git tags, generate changelog and publish GitHub release'

inputs:
  app-name:
    description: 'Application name'
    required: true
  app-version:
    description: 'Application version'
    required: true
  release-name:
    description: 'Release name (will be prefixed to version)'
    required: false
    default: 'Release'
  is-prerelease:
    description: 'Mark as pre-release'
    required: false
    default: 'false'
  make-latest:
    description: 'Mark as latest release'
    required: false
    default: 'true'
  skip-if-exists:
    description: 'Skip if release already exists'
    required: false
    default: 'true'
  artifacts-pattern:
    description: 'Pattern for artifacts to include'
    required: false
    default: '*.zip'
  github-token:
    description: 'GitHub token for API access'
    required: true
  generate-release-notes:
    description: 'Auto-generate release notes from commits'
    required: false
    default: 'true'
  target-commitish:
    description: 'Commitish value for tag creation (defaults to current branch)'
    required: false
    default: ''

outputs:
  release-url:
    description: 'URL of the created release'
    value: ${{ steps.publish-release.outputs.url }}
  tag-name:
    description: 'Name of the created tag'
    value: ${{ inputs.app-version }}
  release-id:
    description: 'ID of the created release'
    value: ${{ steps.publish-release.outputs.id }}
  upload-url:
    description: 'URL for uploading additional assets'
    value: ${{ steps.publish-release.outputs.upload_url }}
  assets:
    description: 'JSON array of uploaded assets information'
    value: ${{ steps.publish-release.outputs.assets }}

runs:
  using: 'composite'
  steps:
    - name: 📋 Release Information
      id: release-info
      shell: bash
      run: |
        TAG_NAME="${{ inputs.app-version }}"
        RELEASE_TITLE="${{ inputs.release-name }} ${{ inputs.app-version }}"

        echo "🚀 Release Information:"
        echo "  • Tag Name: $TAG_NAME"
        echo "  • Release Title: $RELEASE_TITLE"
        echo "  • Pre-release: ${{ inputs.is-prerelease }}"
        echo "  • Make Latest: ${{ inputs.make-latest }}"
        echo "  • Skip if exists: ${{ inputs.skip-if-exists }}"
        echo "  • Auto-generate notes: ${{ inputs.generate-release-notes }}"

        echo "tag-name=$TAG_NAME" >> $GITHUB_OUTPUT
        echo "release-title=$RELEASE_TITLE" >> $GITHUB_OUTPUT

    - name: 🔍 Check if Release Already Exists
      id: check-release
      if: inputs.skip-if-exists == 'true'
      shell: bash
      run: |
        echo "🔍 Checking if release ${{ inputs.app-version }} already exists..."

        if gh release view "${{ inputs.app-version }}" --repo "${{ github.repository }}" > /dev/null 2>&1; then
          echo "⏭️ Release ${{ inputs.app-version }} already exists - skipping creation"
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "✅ Release ${{ inputs.app-version }} does not exist - proceeding with creation"
          echo "exists=false" >> $GITHUB_OUTPUT
        fi
      env:
        GH_TOKEN: ${{ inputs.github-token }}

    - name: 🏷️ Create Git Tag
      if: steps.check-release.outputs.exists != 'true'
      shell: bash
      run: |
        echo "🏷️ Creating git tag for release..."

        git config --local user.email "actions@github.com"
        git config --local user.name "GitHub Actions"

        TAG_NAME="${{ inputs.app-version }}"
        echo "📌 Creating tag: $TAG_NAME"

        # Check if tag already exists locally or remotely
        if git tag -l "$TAG_NAME" | grep -q "^$TAG_NAME$"; then
          echo "⚠️ Tag $TAG_NAME already exists locally"
        elif git ls-remote --tags origin | grep -q "refs/tags/$TAG_NAME$"; then
          echo "⚠️ Tag $TAG_NAME already exists on remote"
        else
          # Create and push the tag
          if [ -n "${{ inputs.target-commitish }}" ]; then
            git tag "$TAG_NAME" "${{ inputs.target-commitish }}"
            echo "✅ Tag $TAG_NAME created at commit ${{ inputs.target-commitish }}"
          else
            git tag "$TAG_NAME"
            echo "✅ Tag $TAG_NAME created at current HEAD"
          fi

          if git push origin "$TAG_NAME"; then
            echo "✅ Tag $TAG_NAME pushed successfully to remote"
          else
            echo "❌ Failed to push tag $TAG_NAME"
            exit 1
          fi
        fi

    - name: � Validate Available Artifacts
      id: validate-artifacts
      if: steps.check-release.outputs.exists != 'true'
      shell: bash
      run: |
        echo "� Validating artifacts matching pattern '${{ inputs.artifacts-pattern }}'..."

        # Create array to store artifact files
        artifacts=()

        # Find all matching files
        for file in ${{ inputs.artifacts-pattern }}; do
          if [ -f "$file" ]; then
            artifacts+=("$file")
          fi
        done

        # Check if we found any artifacts
        if [ ${#artifacts[@]} -eq 0 ]; then
          echo "⚠️ No artifacts found matching pattern '${{ inputs.artifacts-pattern }}'"
          echo "files=" >> $GITHUB_OUTPUT
          echo "count=0" >> $GITHUB_OUTPUT
        else
          echo "✅ Found ${#artifacts[@]} artifact(s):"

          total_size=0
          for file in "${artifacts[@]}"; do
            size=$(stat -c%s "$file" 2>/dev/null || stat -f%z "$file" 2>/dev/null || echo "0")
            size_mb=$((size / 1048576))
            echo "  • $file ($(du -h "$file" | cut -f1))"

            # Validate file integrity for ZIP files
            if [[ "$file" == *.zip ]]; then
              if ! unzip -t "$file" > /dev/null 2>&1; then
                echo "❌ Artifact $file is corrupted (ZIP integrity check failed)"
                exit 1
              fi
            fi

            total_size=$((total_size + size))
          done

          total_size_mb=$((total_size / 1048576))
          echo "📊 Total artifacts size: ${total_size_mb}MB"

          # Convert array to newline-separated string for action-gh-release
          printf -v files_list '%s\n' "${artifacts[@]}"
          echo "files<<EOF" >> $GITHUB_OUTPUT
          echo "$files_list" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "count=${#artifacts[@]}" >> $GITHUB_OUTPUT
        fi

    - name: 🚀 Publish GitHub Release
      id: publish-release
      if: steps.check-release.outputs.exists != 'true'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ inputs.app-version }}
        name: ${{ steps.release-info.outputs.release-title }}
        files: ${{ steps.validate-artifacts.outputs.files }}
        prerelease: ${{ inputs.is-prerelease }}
        make_latest: ${{ inputs.make-latest }}
        generate_release_notes: ${{ inputs.generate-release-notes }}
        target_commitish: ${{ inputs.target-commitish }}
        fail_on_unmatched_files: true
        token: ${{ inputs.github-token }}

    - name: ✅ Release Summary
      if: always()
      shell: bash
      run: |
        TAG_NAME="${{ inputs.app-version }}"
        RELEASE_TITLE="${{ steps.release-info.outputs.release-title }}"

        echo "✅ Release Management Summary:"
        echo "  • Tag: $TAG_NAME"
        echo "  • Release Title: $RELEASE_TITLE"

        if [ "${{ steps.check-release.outputs.exists }}" = "true" ]; then
          echo "  • Status: ⏭️ Skipped (release already exists)"
          echo "  • Existing Release URL: https://github.com/${{ github.repository }}/releases/tag/$TAG_NAME"
        elif [ "${{ steps.publish-release.outcome }}" = "success" ]; then
          echo "  • Status: ✅ Successfully published"
          echo "  • Release URL: ${{ steps.publish-release.outputs.url }}"
          echo "  • Release ID: ${{ steps.publish-release.outputs.id }}"
          echo "  • Upload URL: ${{ steps.publish-release.outputs.upload_url }}"
          echo "  • Assets uploaded: ${{ steps.validate-artifacts.outputs.count }}"
          echo "  • Pre-release: ${{ inputs.is-prerelease }}"
          echo "  • Make Latest: ${{ inputs.make-latest }}"
          echo "🎉 Release published successfully!"
        else
          echo "  • Status: ❌ Failed"
          echo "💥 Release publication failed - check previous steps for errors"
        fi
